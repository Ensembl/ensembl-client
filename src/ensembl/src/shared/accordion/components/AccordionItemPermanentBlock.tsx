import React from 'react';

// This block has to be please either before or after AccordionItemPanel
const AccordionItemPermanentBlock = (props: any) => {
  return <div {...props} />;
};

export default AccordionItemPermanentBlock;
