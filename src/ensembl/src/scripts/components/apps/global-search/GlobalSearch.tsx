import React, { Component } from 'react';
import { hot } from 'react-hot-loader';

class GlobalSearch extends Component {
  public render() {
    return <h2>Global Search Placeholder</h2>;
  }
}

export default hot(module)(GlobalSearch);
